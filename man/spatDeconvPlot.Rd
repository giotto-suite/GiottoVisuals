% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_spatial_gg.R
\name{spatDeconvPlot}
\alias{spatDeconvPlot}
\title{spatDeconvPlot}
\usage{
spatDeconvPlot(
  gobject,
  spat_unit = NULL,
  feat_type = NULL,
  deconv_name = "DWLS",
  show_image = FALSE,
  gimage = NULL,
  image_name = NULL,
  largeImage_name = NULL,
  spat_loc_name = NULL,
  sdimx = "sdimx",
  sdimy = "sdimy",
  cell_color_code = NULL,
  line_color = NA,
  pie_scale = 0.35,
  alpha = 1,
  legend_text = 8,
  background_color = "white",
  title = NULL,
  axis_text = 8,
  axis_title = 8,
  coord_fix_ratio = 1,
  show_plot = NULL,
  return_plot = NULL,
  save_plot = NULL,
  save_param = list(),
  theme_param = list(),
  default_save_name = "spatDeconvPlot",
  view = NULL,
  space = NULL,
  samples = NULL
)
}
\arguments{
\item{gobject}{giotto object}

\item{spat_unit}{spatial unit (e.g. "cell")}

\item{feat_type}{feature type (e.g. "rna", "dna", "protein")}

\item{deconv_name}{name of deconvolution results to use}

\item{show_image}{show a tissue background image}

\item{gimage}{a giotto image}

\item{image_name}{name of a giotto image}

\item{largeImage_name}{name of a giottoLargeImage}

\item{spat_loc_name}{name of spatial locations}

\item{sdimx}{x-axis dimension name (default = 'sdimx')}

\item{sdimy}{y-axis dimension name (default = 'sdimy')}

\item{cell_color_code}{character. discrete colors to use. palette to use or
named vector of colors}

\item{line_color}{color of line within pie charts}

\item{pie_scale}{amount to scale the pie size if there is no radius
mapping exists}

\item{alpha}{alpha of pie charts}

\item{legend_text}{size of legend text}

\item{background_color}{color of plot background}

\item{title}{title for plot (default = deconv_name)}

\item{axis_text}{size of axis text}

\item{axis_title}{size of axis title}

\item{coord_fix_ratio}{fix ratio between x and y-axis}

\item{show_plot}{logical. show plot}

\item{return_plot}{logical. return ggplot object}

\item{save_plot}{logical. save the plot}

\item{save_param}{list of saving parameters,
see \code{\link{showSaveParameters}}}

\item{theme_param}{list of additional params passed to \code{ggplot2::theme()}}

\item{default_save_name}{default save name for saving, don't change,
change save_name in save_param}

\item{view}{character. Name of a \link[GiottoClass:giottoView-class]{GiottoClass::giottoView} slotted on
\code{gobject}. When supplied, the object is narrowed through
\code{\link[GiottoClass:materialize]{GiottoClass::materialize()}} once before any data is fetched, so every slot
read in the plot shares one resolver pass. A recipe object is not accepted —
it has no name, so nothing downstream could say which view a figure came
from.}

\item{space}{character. Name of a \link[GiottoClass:giottoSpace-class]{GiottoClass::giottoSpace} slotted on
\code{gobject}, applied the same way. On a \code{giottoMulti} a
\link[GiottoClass:combinedSpace-class]{GiottoClass::combinedSpace} also supplies the default \code{samples}, since
its members are laid out relative to one another and a sample outside that
layout has no position in the frame. A \link[GiottoClass:perSampleSpace-class]{GiottoClass::perSampleSpace}
does not: its membership is open, so it reframes every panel without deciding
which are drawn.}

\item{samples}{character. On a \code{giottoMulti}, which children to draw one
panel each for. \code{NULL} (default) means all of them, or a \code{combinedSpace}'s
members when \code{space} names one; \code{":all:"} is the explicit form of the
default. Ignored for a plain \code{giotto}.}
}
\value{
ggplot
}
\description{
Visualize cell type enrichment / deconvolution results
in a scatterpie
}
