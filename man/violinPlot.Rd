% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_violin.R
\name{violinPlot}
\alias{violinPlot}
\title{violinPlot}
\usage{
violinPlot(
  gobject,
  spat_unit = NULL,
  feat_type = NULL,
  expression_values = c("normalized", "scaled", "custom"),
  feats = NULL,
  cluster_column,
  cluster_custom_order = NULL,
  color_violin = c("feats", "cluster"),
  cluster_color_code = NULL,
  strip_position = c("top", "right", "left", "bottom"),
  strip_text = 7,
  axis_text_x_size = 10,
  axis_text_y_size = 6,
  show_plot = NULL,
  return_plot = NULL,
  save_plot = NULL,
  save_param = list(),
  default_save_name = "violinPlot",
  view = NULL,
  space = NULL
)
}
\arguments{
\item{gobject}{giotto object}

\item{spat_unit}{spatial unit (e.g. "cell")}

\item{feat_type}{feature type (e.g. "rna", "dna", "protein")}

\item{expression_values}{expression values to
use (e.g. "normalized", "scaled", "custom")}

\item{feats}{features to plot}

\item{cluster_column}{name of column to use for
clusters (e.g. "leiden_clus")}

\item{cluster_custom_order}{custom order of clusters}

\item{color_violin}{color violin according to "genes" or "clusters"}

\item{cluster_color_code}{color code for clusters}

\item{strip_position}{position of gene
labels (e.g. "top", "right", "left", "bottom")}

\item{strip_text}{size of strip text}

\item{axis_text_x_size}{size of x-axis text}

\item{axis_text_y_size}{size of y-axis text}

\item{show_plot}{logical. show plot}

\item{return_plot}{logical. return ggplot object}

\item{save_plot}{logical. save the plot}

\item{save_param}{list of saving parameters,
see \code{\link{showSaveParameters}}}

\item{default_save_name}{default save name for saving, don't change,
change save_name in save_param}

\item{view}{character. Name of a \link[GiottoClass:giottoView-class]{GiottoClass::giottoView} slotted on
\code{gobject}. When supplied, the object is narrowed through
\code{\link[GiottoClass:materialize]{GiottoClass::materialize()}} once before any data is fetched, so every slot
read in the plot shares one resolver pass. A recipe object is not accepted —
it has no name, so nothing downstream could say which view a figure came
from.}

\item{space}{character. Name of a \link[GiottoClass:giottoSpace-class]{GiottoClass::giottoSpace} slotted on
\code{gobject}, applied the same way. On a \code{giottoMulti} a
\link[GiottoClass:combinedSpace-class]{GiottoClass::combinedSpace} also supplies the default \code{samples}, since
its members are laid out relative to one another and a sample outside that
layout has no position in the frame. A \link[GiottoClass:perSampleSpace-class]{GiottoClass::perSampleSpace}
does not: its membership is open, so it reframes every panel without deciding
which are drawn.}
}
\value{
ggplot
}
\description{
Creates violinplot for selected clusters
}
\examples{
g <- GiottoData::loadGiottoMini("visium", verbose = FALSE)
violinPlot(g,
    feats = c("Gna12", "Ccnd2", "Btbd17"),
    cluster_column = "leiden_clus"
)

}
