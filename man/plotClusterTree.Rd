% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dendrogram.R
\name{plotClusterTree}
\alias{plotClusterTree}
\title{plotClusterTree}
\usage{
plotClusterTree(
  gobject,
  spat_unit = NULL,
  feat_type = NULL,
  cluster_column,
  tree,
  labels,
  k = NULL,
  gini_markers = NULL,
  margin_cut = 25,
  node_labels = TRUE,
  palette = NULL,
  label_size = 3,
  base_size = 11,
  show_plot = NULL,
  return_plot = NULL,
  save_plot = NULL,
  save_param = list(),
  default_save_name = "plotClusterTree"
)
}
\arguments{
\item{gobject}{giotto object}

\item{spat_unit}{spatial unit}

\item{feat_type}{feature type}

\item{cluster_column}{name of the cell metadata column holding the clusters}

\item{tree}{an \code{hclust} over the clusters, from
\code{Giotto::calculateClusterTree()}}

\item{labels}{the annotation, as a list with \code{clusters} and optionally
\code{nodes}; the same object \code{Giotto::annotateClusterTree()} takes}

\item{k}{granularities to draw as ladder bands, passed to \code{\link[stats:cutree]{stats::cutree()}}.
The finest is drawn at the top, nearest the leaves.}

\item{gini_markers}{gini marker table carrying \code{detection_margin}, for the
specificity track. Omit to leave that track out.}

\item{margin_cut}{detection margin below which a leaf is marked as having no
feature of its own}

\item{node_labels}{draw the label of each internal node on the tree}

\item{palette}{colours for the coarsest ladder band, recycled across the
others. A named vector keys colours to labels.}

\item{label_size, base_size}{text sizes}

\item{show_plot, return_plot, save_plot, save_param, default_save_name}{plot output settings, see \code{\link[=plot_output_handler]{plot_output_handler()}}}
}
\value{
ggplot
}
\description{
Publication figure for an annotated cluster tree: the
dendrogram, a ladder showing how labels merge as the tree is cut more
coarsely, and per-cluster evidence tracks.
}
\details{
The figure answers a question a bare dendrogram cannot: \strong{at what level
should these clusters be annotated?} The ladder makes the trade-off legible
— read down to see distinct labels collapse into one, read up to see a label
split into types that the data may or may not support — and the tracks put
the evidence for each leaf next to the name it was given, so a small cluster
with no marker of its own is visible rather than implied.
}
\examples{
g <- GiottoData::loadGiottoMini("visium")

tree <- Giotto::calculateClusterTree(g, cluster_column = "leiden_clus")
labs <- list(clusters = stats::setNames(
    paste("type", tree$labels), tree$labels
))
plotClusterTree(g,
    cluster_column = "leiden_clus", tree = tree,
    labels = labs, k = c(2, 4)
)
}
\seealso{
\code{Giotto::calculateClusterTree()},
\code{Giotto::annotateClusterTree()}, \code{\link[=showClusterDendrogram]{showClusterDendrogram()}}
}
